# TODOS

departement, employees, ledger, 

- `buntcd`        → base unit code
- `ledcrtdt`      → creation date
- `exmpt`         → exempted (Yes/No)
- `rof`           → round off (store as Int: 1=Yes, 0=No) OR repurpose a String field
- `ledsts`        → ledger status (ACTIVE/INACTIVE/LOCKED)
- `pcatcd`        → party category
- `per1prfx`      → contact person 1
- `ctper1`        → contact mobile 1
- `cntno1`        → contact email 1  ← repurposed
- `per2prfx`      → contact person 2
- `ctper2`        → contact mobile 2
- `cntno2`        → contact email 2  ← repurposed
- `ledadr1`       → billing address
- `areacd`        → billing area code
- `ctycd`         → billing city code
- `pincd`         → billing pin
- `loccd`         → billing state (repurposed, store stcd here)
- `empcd`         → employee
- `ledadr2`       → shipping address
- `ctycd2`        → shipping city code
- `pincd2`        → shipping pin
- `paytyp`        → pay type
- `bnkledcd`      → bank name/ledger
- `accno`         → account number
- `paystncd`      → pay station
- `rtgsno`        → RTGS number
- `panno/pandt`   → PAN
- `tanno/tandt`   → TAN
- `stxno/stxdt`   → Service Tax
- `lmtamt`        → credit limit amount
- `consdays`      → consignment days
