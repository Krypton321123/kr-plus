BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[userScreen] ADD [viewOnly] BIT NOT NULL CONSTRAINT [userScreen_viewOnly_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
